# Core
pydantic>=2.5
httpx>=0.27
pillow>=10.0
numpy>=1.26

# OCR · Chinese digit recognition (CPU build is enough · GPU build paddlepaddle-gpu is optional)
paddlepaddle>=2.6
paddleocr>=3.0

# CV · UI element detection (needed by arrow_finder)
opencv-python>=4.9

# Optional · extracting frames directly from an mp4 recording requires ffmpeg on the system
# Optional · running a local VLM needs vllm · install it separately per https://docs.vllm.ai/

# S16 patch knowledge base · from the local jcc-daida project (no setup.py · loaded by file path)
# Usage: set the env var JCC_DAIDA_PATH=/path/to/jcc-daida · or keep the default and let src/knowledge.py
# use the hardcoded dev path. When not found, the Analyzer degrades silently · does not block the pipeline.
# jcc-daida runtime dependency:
pyyaml>=6.0

# Real-time coach · advice broadcast service
fastapi>=0.115
uvicorn[standard]>=0.32

# Testing
pytest>=8.0
